import Link from "next/link";
export default async function Confirmado({params,searchParams}:{params:Promise<{token:string}>;searchParams:Promise<{pedido?:string}>}){const {token}=await params;const q=await searchParams;return <main className="grid min-h-screen place-items-center bg-emerald-50 p-6"><div className="max-w-md rounded-3xl bg-white p-8 text-center shadow-xl"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-emerald-100 text-3xl">✓</div><h1 className="mt-4 text-3xl font-bold">Pedido enviado!</h1><p className="mt-2 text-gray-500">Pedido #{q.pedido||""} foi encaminhado para a cozinha. Você pode adicionar mais itens na mesma comanda.</p><Link href={`/mesa/${token}`} className="mt-6 inline-block rounded-xl bg-emerald-700 px-5 py-3 font-semibold text-white">Voltar ao cardápio</Link></div></main>}
