import { Phone, ShieldCheck } from "lucide-react";
import { requirePlatformStaff } from "@/lib/master/auth";
import { addTrialDays, setModuleOverride, updateSubscription } from "./actions";
const statusLabel:Record<string,string>={trialing:"Em teste",active:"Pago/ativo",past_due:"Pagamento pendente",suspended:"Suspenso",canceled:"Cancelado"};
const modules=["dashboard","orders","products","kitchen","delivery","payments","finance","reports","stock","customers","promotions","marketing","messages","tables","settings","team"];
const date=(value?:string|null)=>value?new Date(value).toLocaleString("pt-BR",{dateStyle:"short",timeStyle:"short"}):"—";
export default async function CompaniesPage(){const {admin,staff}=await requirePlatformStaff();const [{data:companies},{data:plans},{data:subscriptions},{data:overrides},{data:authUsers}]=await Promise.all([admin.from("companies").select("id,owner_id,name,responsible_name,phone,whatsapp,status,created_at,last_activity_at").order("created_at",{ascending:false}),admin.from("subscription_plans").select("id,code,name").eq("is_active",true).order("name"),admin.from("company_subscriptions").select("company_id,plan_id,status,trial_ends_at,current_period_starts_at,current_period_ends_at"),admin.from("company_entitlement_overrides").select("company_id,module_key,enabled"),admin.auth.admin.listUsers({page:1,perPage:1000})]);const subMap=new Map((subscriptions||[]).map(s=>[s.company_id,s]));const userMap=new Map((authUsers?.users||[]).map(u=>[u.id,u]));return <div className="space-y-6"><header><p className="font-semibold text-emerald-700">Clientes da plataforma</p><h1 className="text-3xl font-black">Empresas</h1><p className="text-gray-500">Plano, teste, contato e última atividade.</p></header><div className="grid gap-4 md:hidden">{companies?.map(company=>{const sub=subMap.get(company.id);const plan=plans?.find(p=>p.id===sub?.plan_id);const owner=userMap.get(company.owner_id);return <article key={company.id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex justify-between gap-3"><div><h2 className="font-bold">{company.name}</h2><p className="text-sm text-gray-500">{company.responsible_name||owner?.user_metadata?.full_name||owner?.email||"Responsável não informado"}</p></div><span className="h-fit rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{statusLabel[sub?.status||""]||"Sem assinatura"}</span></div><p className="mt-4 flex gap-2 text-sm"><Phone size={16}/>{company.whatsapp||company.phone||"Sem contato"}</p><p className="mt-2 text-sm">Plano: <b>{plan?.name||"—"}</b></p><p className="mt-1 text-xs text-gray-500">Teste: {date(sub?.trial_ends_at)} • Atividade: {date(company.last_activity_at)}</p>{staff.support_level==="master"&&<CompanyControls companyId={company.id} subscription={sub} plans={plans||[]} overrides={(overrides||[]).filter(o=>o.company_id===company.id)}/>}</article>})}</div><div className="hidden overflow-x-auto rounded-2xl border bg-white md:block"><table className="w-full min-w-[1100px] text-left text-sm"><thead><tr className="border-b bg-slate-50 text-gray-500"><th className="p-4">Empresa / responsável</th><th>Contato</th><th>Plano</th><th>Status</th><th>Início</th><th>Fim do teste</th><th>Última atividade</th><th>Ações</th></tr></thead><tbody>{companies?.map(company=>{const sub=subMap.get(company.id);const plan=plans?.find(p=>p.id===sub?.plan_id);const owner=userMap.get(company.owner_id);return <tr key={company.id} className="border-b align-top"><td className="p-4"><b>{company.name}</b><p className="max-w-[220px] truncate text-xs text-gray-500">{company.responsible_name||owner?.user_metadata?.full_name||owner?.email||"Não informado"}</p></td><td>{company.whatsapp||company.phone||"—"}</td><td>{plan?.name||"—"}</td><td><span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold">{statusLabel[sub?.status||""]||"Sem assinatura"}</span></td><td>{date(sub?.current_period_starts_at||company.created_at)}</td><td>{date(sub?.trial_ends_at)}</td><td>{date(company.last_activity_at)}</td><td className="py-3">{staff.support_level==="master"?<CompanyControls companyId={company.id} subscription={sub} plans={plans||[]} overrides={(overrides||[]).filter(o=>o.company_id===company.id)}/>:<span className="text-xs text-gray-500">Somente leitura</span>}</td></tr>})}</tbody></table></div></div>}
function CompanyControls({companyId,subscription,plans,overrides}:{companyId:string;subscription:any;plans:Array<{id:string;name:string}>;overrides:Array<{module_key:string;enabled:boolean}>}){return <details className="mt-3"><summary className="cursor-pointer font-bold text-emerald-700">Administrar</summary><div className="mt-3 w-[min(88vw,420px)] space-y-4 rounded-xl border bg-white p-4 shadow-lg"><form action={updateSubscription} className="grid gap-2 sm:grid-cols-2"><input type="hidden" name="companyId" value={companyId}/><select name="planId" defaultValue={subscription?.plan_id} className="rounded-lg border px-3 py-2">{plans.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select name="status" defaultValue={subscription?.status||"trialing"} className="rounded-lg border px-3 py-2"><option value="trialing">Em teste</option><option value="active">Pago/ativo</option><option value="past_due">Pagamento pendente</option><option value="suspended">Suspenso</option><option value="canceled">Cancelado</option></select><button className="rounded-lg bg-[#063D2F] px-3 py-2 font-bold text-white sm:col-span-2">Salvar plano e status</button></form><form action={addTrialDays} className="flex gap-2"><input type="hidden" name="companyId" value={companyId}/><input name="days" type="number" min="1" max="90" defaultValue="3" className="min-w-0 flex-1 rounded-lg border px-3 py-2"/><button className="rounded-lg bg-orange-500 px-3 py-2 font-bold text-white">Adicionar dias</button></form><details><summary className="cursor-pointer text-sm font-bold">Liberar ou bloquear módulos</summary><div className="mt-2 grid gap-2 sm:grid-cols-2">{modules.map(moduleKey=>{const override=overrides.find(o=>o.module_key===moduleKey);return <form action={setModuleOverride} key={moduleKey} className="flex items-center justify-between rounded-lg bg-slate-50 p-2 text-xs"><input type="hidden" name="companyId" value={companyId}/><input type="hidden" name="moduleKey" value={moduleKey}/><span>{moduleKey}{override?` (${override.enabled?"livre":"bloqueado"})`:""}</span><div className="flex gap-1"><button name="enabled" value="true" title="Liberar" className="rounded bg-emerald-100 p-1 text-emerald-800"><ShieldCheck size={15}/></button><button name="enabled" value="false" title="Bloquear" className="rounded bg-red-100 p-1 text-red-800">×</button></div></form>})}</div></details></div></details>}
