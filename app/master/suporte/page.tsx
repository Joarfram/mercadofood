import { Headphones, KeyRound, ShieldCheck } from "lucide-react";
import { requirePlatformStaff } from "@/lib/master/auth";
import { enterSupport } from "./actions";
export default async function MasterSupport({searchParams}:{searchParams:Promise<{erro?:string;encerrado?:string}>}){const {admin,staff}=await requirePlatformStaff();const query=await searchParams;const {data:logs}=await admin.from("support_audit_logs").select("id,action,table_name,occurred_at,companies(name),platform_staff(display_name)").order("occurred_at",{ascending:false}).limit(30);return <div className="space-y-7"><header><h1 className="text-3xl font-black">Central de suporte</h1><p className="text-gray-500">Entre somente com autorização temporária do cliente.</p></header>{query.erro&&<p className="rounded-xl bg-red-50 p-4 font-semibold text-red-700">{query.erro}</p>}{query.encerrado&&<p className="rounded-xl bg-emerald-50 p-4 font-semibold text-emerald-700">Sessão de suporte encerrada.</p>}<section className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><form action={enterSupport} className="rounded-2xl bg-[#063D2F] p-6 text-white shadow-xl"><KeyRound className="text-orange-400" size={32}/><h2 className="mt-4 text-xl font-bold">Código do cliente</h2><p className="mt-1 text-sm text-emerald-100">Digite os 6 números exibidos em Configurações → Suporte.</p><input name="code" inputMode="numeric" pattern="[0-9]{6}" maxLength={6} required className="mt-5 w-full rounded-xl bg-white px-4 py-4 text-center text-3xl font-black tracking-[.25em] text-slate-950" placeholder="000000"/><button className="mt-3 flex w-full items-center justify-center gap-2 rounded-xl bg-orange-500 px-4 py-3 font-bold"><Headphones size={18}/>Iniciar atendimento</button>{staff.support_level==="viewer"&&<p className="mt-3 text-sm text-amber-200">Seu nível permite resgatar somente códigos de visualização.</p>}</form><article className="rounded-2xl border bg-white p-6"><ShieldCheck className="text-emerald-700"/><h2 className="mt-3 text-xl font-bold">Proteções ativas</h2><ul className="mt-4 space-y-3 text-sm text-gray-600"><li>• Código de uso único e validade de 30 minutos.</li><li>• Sessão limitada à empresa que gerou o código.</li><li>• Nenhuma senha do cliente é compartilhada.</li><li>• Toda alteração registra usuário, horário e antes/depois.</li><li>• Faixa visível durante todo o atendimento.</li></ul></article></section><section className="rounded-2xl border bg-white p-5"><h2 className="text-xl font-bold">Auditoria recente</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead><tr className="border-b text-gray-500"><th className="py-3">Data</th><th>Empresa</th><th>Atendente</th><th>Ação</th><th>Tabela</th></tr></thead><tbody>{logs?.map(log=><tr key={log.id} className="border-b"><td className="py-3">{new Date(log.occurred_at).toLocaleString("pt-BR")}</td><td>{(Array.isArray(log.companies)?log.companies[0]:log.companies)?.name||"—"}</td><td>{(Array.isArray(log.platform_staff)?log.platform_staff[0]:log.platform_staff)?.display_name||"—"}</td><td>{log.action}</td><td>{log.table_name||"—"}</td></tr>)}</tbody></table></div></section></div>}
