import { updatePassword } from "./actions";
import { PasswordInput } from "@/components/auth/password-input";
export default async function UpdatePasswordPage({ searchParams }: { searchParams: Promise<{ erro?: string }> }) { const q = await searchParams; return <main className="grid min-h-screen place-items-center bg-[#f7faf8] p-5"><form action={updatePassword} className="w-full max-w-md rounded-3xl border bg-white p-7 shadow-xl"><h1 className="text-2xl font-bold">Crie uma nova senha</h1>{q.erro && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{q.erro}</p>}<label className="mt-5 block text-sm font-semibold">Nova senha<PasswordInput name="password" minLength={8} required autoComplete="new-password" className="w-full rounded-xl border px-4 py-3"/></label><label className="mt-4 block text-sm font-semibold">Confirmar senha<PasswordInput name="confirm" minLength={8} required autoComplete="new-password" className="w-full rounded-xl border px-4 py-3"/></label><button className="mt-5 w-full rounded-xl bg-emerald-700 py-3 font-bold text-white">Salvar nova senha</button></form></main>; }
