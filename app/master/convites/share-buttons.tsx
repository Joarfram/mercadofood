"use client";
import { useState } from "react";
export function ShareButtons({link,email,whatsapp}:{link:string;email:string;whatsapp:string}){const [copied,setCopied]=useState(false);const message=`Olá! Seu acesso ao MercadoFood está pronto. Crie sua conta por este link: ${link}`;return <div className="mt-3 flex flex-wrap gap-2"><button type="button" className="rounded-lg bg-slate-900 px-4 py-2 text-white" onClick={async()=>{await navigator.clipboard.writeText(link);setCopied(true);setTimeout(()=>setCopied(false),2000)}}>{copied?"Link copiado!":"Copiar link"}</button><a className="rounded-lg bg-emerald-700 px-4 py-2 text-white" href={`https://wa.me/${whatsapp}?text=${encodeURIComponent(message)}`} target="_blank" rel="noreferrer">Abrir WhatsApp do cliente</a><a className="rounded-lg border px-4 py-2" href={`mailto:${email}?subject=${encodeURIComponent("Seu acesso ao MercadoFood")}&body=${encodeURIComponent(message)}`}>Abrir e-mail do cliente</a></div>}
